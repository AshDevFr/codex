# Codex Configuration with SQLite

database:
  db_type: sqlite
  sqlite:
    path: ./data/codex.db
    pragmas:
      journal_mode: WAL
      synchronous: NORMAL
      foreign_keys: "ON"

application:
  name: Codex
  host: 127.0.0.1
  port: 8080
  debug: false
